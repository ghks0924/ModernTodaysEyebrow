package com.todayseyebrow.moderntodayseyebrow.ui.adapter

import androidx.recyclerview.widget.RecyclerView
import com.todayseyebrow.moderntodayseyebrow.data.model.Menu
import com.todayseyebrow.moderntodayseyebrow.databinding.RvItemMenuBinding

class MenuViewHolder(
    private val binding: RvItemMenuBinding
) : RecyclerView.ViewHolder(binding.root) {

    fun bind(menu: Menu) {
//        val title = if (menu.memoTitle.isNotEmpty()) memo.memoTitle else ""
//        val content = if (memo.memoContent.isNotEmpty()) memo.memoContent else ""

        itemView.apply {


        }

    }
}